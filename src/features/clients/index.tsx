import { FeaturePlaceholder } from '../_shared/FeaturePlaceholder'

const ClientsPage = () => {
  return <FeaturePlaceholder title="Clients" description="This feature shell will host client management in a later milestone." />
}

export default ClientsPage
